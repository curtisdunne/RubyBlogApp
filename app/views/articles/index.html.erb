<h1 align="center">List of all Articles</h1>

<%= will_paginate @articles, list_classes: %w(pagination justify-content-center), class:"pg-teal" %>

<%= render 'article', obj: @articles %>

<%= will_paginate @articles, list_classes: %w(pagination justify-content-center) %>
